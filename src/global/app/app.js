import { $routeConfig } from './routes';

export { config } from './config';

export var dependencies = [
	'ui.router',
	'ngAnimate',
	'ngAria',
	'ngMaterial',
	'ngResource'
];

export class AppCtrl {

	static $inject = [];
	constructor() {

	}
}